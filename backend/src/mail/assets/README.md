# 메일 HTML 배너용 자산

- `samkwang-mail-logo.png` — SAMKWANG 로고(프론트 `public/samkwang-logo.png`와 동일 소스). `nest build` 시 `dist/mail/assets/`로 복사되며, 발송 HTML에서 CID로 참조됩니다.
- 로고를 교체할 때는 이 파일을 덮어쓴 뒤 백엔드를 다시 빌드하세요.
